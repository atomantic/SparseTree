# All local data (family trees, SQLite db, credentials, photos, etc.)
data/
# Local data backups (e.g. created before a rebuild/migration)
data_old/
# Data archives/backups (may contain personal family data)
data*.zip
*.zip

# Browser automation (Chrome profile with auth cookies)
.browser/data/

# Dependencies
node_modules/

# Build outputs
server/dist/
client/dist/
shared/types/
*.tsbuildinfo

# Test outputs (generated reports, not source)
test-results/
client/public/code-coverage/
client/public/coverage-report/
client/public/playwright-report/

# IDE
.vscode/
.idea/

# Screenshots (may contain personal family data)
images/
.playwright-mcp

# Terminal themes
.ghostty-theme